namespace PlatformServiceTemplate.Infrastructure.IntegrationTests;

public sealed class PostgresContainerPlaceholderTests
{
    [Fact(Skip = "Enable Docker in CI to run PostgreSQL Testcontainers integration tests.")]
    public void PostgresContainerBasedTests_ArePlanned()
    {
        Assert.True(true);
    }
}
